---
import Layout from '../layouts/Layout.astro';
import SectionTitle from '../components/SectionTitle.astro';
import MotionReveal from '../components/MotionReveal.astro';
---

<Layout title="About" description="关于一鸣私人摄影馆。">
  <section class="page-section about-page">
    <MotionReveal>
      <SectionTitle label="About" title="关于这个摄影馆" />
    </MotionReveal>
    <div class="about-grid">
      <MotionReveal>
        <div class="about-copy">
          <p>
            一鸣私人摄影馆不是商业作品集，也不是把所有照片堆在一起的相册。它更像一间慢慢布置的私人展馆：一些经过的地方，一些光线，一些城市里的片刻，被整理成展厅、展览和观看顺序。
          </p>
          <p>
            这里的照片不急着证明什么。它们只是从日常里被取出来，获得一点留白、一点文字和一条更安静的观看路径。
          </p>
        </div>
      </MotionReveal>
      <MotionReveal>
        <aside class="about-note">
          <h2>拍摄者</h2>
          <p>私人记录者。持续整理正在发生和已经经过的影像。</p>
          <h2>器材</h2>
          <p>Sony A7M4 / Mobile / Others as needed</p>
        </aside>
      </MotionReveal>
    </div>
  </section>
</Layout>

<style>
  .about-page {
    padding-top: var(--space-8);
  }

  .about-grid {
    display: grid;
    grid-template-columns: minmax(0, 1fr) minmax(260px, 0.38fr);
    gap: clamp(2rem, 6vw, 6rem);
    align-items: start;
  }

  .about-copy {
    max-width: 46rem;
    color: var(--color-muted);
    font-size: var(--step-1);
    line-height: 1.95;
  }

  .about-copy p {
    margin: 0 0 var(--space-4);
  }

  .about-note {
    padding-top: var(--space-3);
    border-top: 1px solid var(--color-line);
    color: var(--color-muted);
  }

  .about-note h2 {
    margin: var(--space-4) 0 var(--space-2);
    color: var(--color-ink);
    font-family: var(--font-mono);
    font-size: 0.76rem;
    font-weight: 500;
    text-transform: uppercase;
  }

  .about-note h2:first-child {
    margin-top: 0;
  }

  .about-note p {
    margin: 0;
  }

  @media (max-width: 780px) {
    .about-grid {
      grid-template-columns: 1fr;
    }
  }
</style>
